imap:
  host: imap.example.com
  port: 993
  tls: true
  starttls: false
  insecure_skip_verify: false
smtp:
  host: smtp.example.com
  port: 587
  tls: false
  starttls: true
  insecure_skip_verify: false
auth:
  username: you@example.com
  # password is stored in keyring backend (set to keychain on macOS)
keyring_backend: file
defaults:
  drafts_mailbox: Drafts
